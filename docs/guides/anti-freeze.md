# 防冻结指南（新号必看）

Telegram 对新号/风控号的限制比较敏感；本项目提供了很多“批量/高频”能力，请务必谨慎使用。

- ⚠️ 新号切记：登录面板后不要进行任何操作，至少养 24 小时再创建频道/群、批量邀请/加管理员等敏感操作
- ✅ 如果是购买的协议号/接码号（需要尽快验号）：建议导入后先做一次「批量 Telegram 状态检测」，必要时用「深度检测」快速筛掉废号（避免隔天再找商家被拒赔付）；验号通过后再开始低频养号与后续操作
- ⚠️ 重要提醒（买号必看）：如果你是用商家提供的 Session 导入到系统里，本质上是在使用“同一个会话”。因此“踢设备”只能踢掉除当前 Session 会话之外的其他设备；若商家仍保留该 Session，依然有被重复使用的风险。想要更安全：务必用验证码登录一次（以你自己的登录为准），再踢出其他 Session 会话，并绑定 2FA 找回邮箱
- ✅ 养号完成后：先从少量、低频操作开始，逐步增加频率（宁可慢点，也别一上来就批量）
- ⛔ 如果账号出现限制/冻结迹象：建议先停用该账号，等待恢复后再继续操作
- 📧 重要账号务必绑定 2FA 找回邮箱：尤其是接码手机号的账号，存在“官方客户端也可能突然掉登录”的情况；若未绑定邮箱，掉线后可能无法找回（账号就丢了）。建议准备一个稳定的域名邮箱，把重要账号都绑定到同一域名邮箱体系，便于后续通过邮箱验证码重新登录。非重要账号可酌情忽略
- 📧 批量换绑邮箱（找回+登录）：账号列表的“批量换绑邮箱（Cloud Mail）”会按账号逐个提交后端处理，并在弹窗里显示当前进度；这样即使选择多个账号，也不会让一个 HTTP 长连接一直等待全部账号完成。成功标准：结果表每个账号都有 success/failed 明细，成功项显示找回邮箱/登录邮箱已确认；若单账号请求仍出现 Network Error，前端会停止剩余账号并标记“未执行”，避免后端幽灵任务仍在跑时叠加新操作。排障：先检查 Cloud Mail 收件箱是否已有 Telegram 邮件、URL/Token/域名是否正确、收码超时是否过长、反代超时是否低于单账号处理时间；回滚方式是重新对失败或未执行账号发起批量换绑，不会删除或停用账号。
- 📧 自动更改登录邮箱：任务中心可以创建「自动更改登录邮箱」计划任务，用于扫描 777000 系统通知中类似 `Settings > Privacy & Security > Login Email` 的登录邮箱重置提示。默认窗口是距今 6 天、前后 12 小时（对应 7 天等待期剩余约 1 天）；只有匹配通知的账号才会发送/确认登录邮箱验证码。邮箱域名可以填写单个 CloudMail 域名，也可以填写多域名池；系统会先读取账号当前登录邮箱掩码，能识别原域名且域名池有其它域名时，会优先随机选一个不同域名生成 `手机号数字@目标域名`，用于刷新邮箱重置时间。前置条件：已在设置页配置 Cloud Mail URL、Token、邮箱域名，账号分类未排除运营，账号 Session 可连接。成功标准：账号详情可看到当前登录邮箱状态，任务详情中每个账号出现 success/skipped/failed 明细，且成功项显示“登录邮箱已确认”。排障：若全部跳过，检查 Cloud Mail 配置、账号分类、通知窗口与 777000 文案；若收码超时，检查 Cloud Mail 收件箱、域名解析和 Telegram 邮件延迟。回滚：任务不会删除/停用账号；如需恢复邮箱，只能对账号重新执行登录邮箱换绑或在 Telegram 客户端中手动修改。

